# ISOs are large — download separately
*.iso

# Generated archives (except live injection hooks)
*.cpio.gz

# Static binaries (download or build separately)

# Network configs may contain secrets
Network/
CLAUDE.md
